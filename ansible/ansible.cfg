[defaults]
# Disable host key checking for SSH connections
host_key_checking = False

# Set the inventory file location
inventory = ./hosts

# Use Python 3 on the remote host
interpreter_python = /usr/bin/python3

# Display output in a more readable format
stdout_callback = yaml

# Don't create .retry files on failure
retry_files_enabled = False
